import { project, participant } from "./main";

async function performTrial(trialToPerform: Trial) {
    for (let i = 0; i < trialToPerform.gestures.length; i++) {
        const gestureToPerform = trialToPerform.gestures[i];
    }
}

async function performGesture(gesture: Gesture, index: number) {
    
}